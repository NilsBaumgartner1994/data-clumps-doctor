import type { ScenarioConfig } from '@/scenarioUtils';

const config = {
  "id": "parameter-field-variable-order-positive-typescript",
  "name": "TypeScript parameter-field data clump with variable order",
  "language": "typescript",
  "sourceDir": "source",
  "expectedReportFile": "report-expected.json",
  "detectorOptions": {}
} satisfies ScenarioConfig;

export default config;
