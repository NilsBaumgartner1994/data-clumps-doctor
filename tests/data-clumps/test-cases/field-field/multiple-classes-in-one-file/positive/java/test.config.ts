import type { ScenarioConfig } from '@/scenarioUtils';

const config = {
  "name": "Java field-field data clump multiple classes in one file",
  "language": "java",
  "sourceDir": "source",
  "expectedReportFile": "report-expected.json",
  "detectorOptions": {}
} satisfies ScenarioConfig;

export default config;
